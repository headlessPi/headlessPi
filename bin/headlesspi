#!/usr/bin/env node

var forever = require('forever-monitor');

var child = new (forever.Monitor)('server.js', {
  silent: false,
  watch:true,
  watchDirectory: 'apps',
  watchIgnorePatterns: ['_*'],
  args: []
});

child.start();

